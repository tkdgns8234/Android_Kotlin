<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:paddingBottom="15dp">

    <EditText
        android:id="@+id/et_dialog_memo"
        android:layout_width="match_parent"
        android:layout_height="60dp"
        android:layout_margin="15dp"
        android:background="@android:color/transparent"
        android:fontFamily="@font/bmjua_ttf"
        android:hint="운동 메모를 입력해주세요"
        android:textSize="25sp"
        app:layout_constraintTop_toTopOf="parent" />

    <View
        android:id="@+id/view"
        android:layout_width="match_parent"
        android:layout_height="1dp"
        android:layout_margin="15dp"
        android:background="#999999"
        app:layout_constraintTop_toBottomOf="@+id/et_dialog_memo" />

    <Button
        android:id="@+id/btn_dialog_selectDate"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_margin="15dp"
        android:fontFamily="@font/bmjua_ttf"
        android:text="@string/select_date"
        app:layout_constraintTop_toBottomOf="@+id/view"
        android:background="@color/orange"/>

    <Button
        android:id="@+id/btn_dialog_save"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_margin="15dp"
        android:fontFamily="@font/bmjua_ttf"
        android:text="메모 저장"
        app:layout_constraintTop_toBottomOf="@+id/btn_dialog_selectDate"
        android:background="@color/orange"/>

</androidx.constraintlayout.widget.ConstraintLayout>